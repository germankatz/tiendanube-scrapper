# URL base del sitio (sin barra final). Se usa para completar enlaces relativos.
BASE_URL=https://ejemplo.com

# Archivo HTML que contiene el listado de productos (para --get-urls)
INPUT_HTML=items.txt

# Archivo donde se guardan las URLs extraídas
OUTPUT_URLS=urls.txt

# Carpeta donde se descargan las fotos y datos.json de cada producto
CARPETA_PRODUCTOS=productos

# Archivo donde se guarda el progreso (para retomar si se corta el scrapper)
ARCHIVO_PROGRESO=progreso_urls.txt
